# CVP React prototype scaffold

Copy this directory for an agent-generated prototype, replace the approved package version and commit SHA placeholders, and update `cvp-provenance.json` after every composition change.

Approved components belong in `assets.approved`. Reusable gaps must be packaged as candidates; product-specific UI belongs in `assets.local`. Never describe either category as approved.
