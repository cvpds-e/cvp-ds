# Candidate components

Reusable proposals awaiting Design System review live here. Each candidate must include a manifest conforming to `src/agent/schemas/candidate-component.schema.json`, implementation, examples, accessibility evidence, and tests.

Candidates must not be exported from `src/design-system/index.ts` until human review changes their registry lifecycle to `approved`.
