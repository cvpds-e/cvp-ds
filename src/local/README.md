# Local prototype UI

Product-specific UI that is not intended for Design System reuse lives here when a prototype is hosted in this repository. Local assets must be recorded in prototype provenance and must not be exported from `src/design-system/index.ts`.
