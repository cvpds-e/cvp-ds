import { PrimaryButton, TextInput } from '@cvp/design-system';

export function PrototypeApp() {
  return (
    <main>
      <h1>CVP prototype</h1>
      <TextInput label="Prototype field" />
      <PrimaryButton>Continue</PrimaryButton>
    </main>
  );
}
